from os import walk
import json

# load all json images of same query
# process all images the like the following
# create class to compute features from every obs.
# according to features found in paper


